ssh-keygen                                        # generate key pair
ssh-copy-id -i ~/.ssh/mykey user@host             # copy public key associated with provided private key to target host as a user
